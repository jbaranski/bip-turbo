{
  "name": "bip-turbo",
  "private": true,
  "scripts": {
    "dev": "bun --cwd apps/web run dev & bun --cwd apps/workers run dev",
    "build": "cd apps/web && bun run build && cd ../workers && bun run build",
    "lint": "bun --cwd apps/web run lint && bun --cwd apps/workers run lint",
    "format": "bun --cwd apps/web run format && bun --cwd apps/workers run format",
    "typecheck": "bun --cwd apps/web run typecheck && bun --cwd apps/workers run typecheck"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "shadcn": "2.4.0-canary.9",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "overrides": {
      "zod": "^3.24.2",
      "pg": "^8.13.3",
      "@types/pg": "^8.11.11"
    }
  },
  "packageManager": "bun@1.2.4",
  "dependencies": {
    "@hookform/resolvers": "^4.1.3",
    "react-hook-form": "^7.54.2"
  }
}
