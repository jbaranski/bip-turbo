{
  "name": "@bip/database",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "doppler run -- prisma generate",
    "db:push": "doppler run -- prisma db push",
    "db:studio": "doppler run -- prisma studio",
    "db:migrate": "doppler run -- prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}